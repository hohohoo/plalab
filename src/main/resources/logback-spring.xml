<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <conversionRule conversionWord="clr"
                    converterClass="org.springframework.boot.logging.logback.ColorConverter"/>
    <conversionRule conversionWord="wex"
                    converterClass="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter"/>

    <property name="CONSOLE_LOG_PATTERN"
              value="%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(%5p) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n%wex"/>
    <property name="DEFAULT_LOG_PATTERN"
              value="%clr([%d{yyyy-MM-dd HH:mm:ss.SSS}]){faint} %clr(%5p) %clr(%msg){magenta} %clr([%15.15t]){faint}%n"/>

    <springProperty name="isFile" source="logging.file"/>
    <springProperty name="logLocation" source="logging.path"/>
    <springProperty name="logLevel" source="logging.level"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>utf8</charset>
        </encoder>
    </appender>

    <appender name="DEFAULT_APPENDER" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${DEFAULT_LOG_PATTERN}</pattern>
            <charset>utf8</charset>
        </encoder>
    </appender>

            <appender name="restAppender" class="ch.qos.logback.core.rolling.RollingFileAppender">
                <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                    <FileNamePattern>${logLocation}/rest.%d{yyyy-MM-dd}.log</FileNamePattern>
                </rollingPolicy>
                <encoder>
                    <pattern>${DEFAULT_LOG_PATTERN}</pattern>
                    <charset>utf8</charset>
                </encoder>
            </appender>

            <appender name="applicationAppender" class="ch.qos.logback.core.rolling.RollingFileAppender">
                <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                    <FileNamePattern>${logLocation}/application.%d{yyyy-MM-dd}.log</FileNamePattern>
                </rollingPolicy>
                <encoder>
                    <pattern>${DEFAULT_LOG_PATTERN}</pattern>
                    <charset>utf8</charset>
                </encoder>
            </appender>

            <logger name="restLogger" additivity="false" level="INFO">
                <appender-ref ref="restAppender"/>
            </logger>

            <logger name="applicationLogger" additivity="false" level="INFO">
                <appender-ref ref="applicationAppender"/>
            </logger>


    <springProfile name="local">
        <logger name="org.springframework" level="INFO"/>
    </springProfile>

    <root level="${logLevel}">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>